# Names only. Never commit real values.
# Optional: used only when a separate config explicitly selects the OpenAI provider.
OPENAI_API_KEY=

# Optional: required only after a licensed market-data adapter is enabled.
FMP_API_KEY=

# Reserved for external operators; the bundled workflow uses the Pages deployment URL.
PAGES_BASE_URL=
